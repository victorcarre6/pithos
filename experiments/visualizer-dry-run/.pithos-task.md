# Current task — band smoothing

Modify only `src/audio_visualizer.py`.

Add the public pure function `smooth_levels(previous, current, alpha)`. `previous` and `current` each contain
exactly the three scalar levels `(bass, mid, treble)`. Return a tuple of three values computed independently
as `previous_value * (1 - alpha) + current_value * alpha`.

For this micro-rush, inputs are already valid and `alpha` is between `0.0` and `1.0`; do not add speculative
validation. Preserve `split_bands` unchanged, use no dependency, and do not edit tests or documentation.
Finish immediately after the implementation.
