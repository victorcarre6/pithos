# Current task

Modify only `tests/test_audio_visualizer.py`.

Rewrite the stale test file for the existing public function `src.audio_visualizer.split_bands`. The function
returns exactly three scalar floats `(bass, mid, treble)`, each the arithmetic mean of one contiguous third.
Cover empty input, silence, all ones, and isolated bass, mid and treble activation.

The test file must run directly with `python tests/test_audio_visualizer.py`. Use only the Python standard
library or plain assertions. Since direct execution puts `tests/` on `sys.path`, insert the project root from
`Path(__file__).resolve().parents[1]` before importing `src.audio_visualizer`. Import only `split_bands`.
Do not modify the implementation, create helpers outside the test file, add dependencies, or edit
documentation. Finish immediately after editing the target file.
