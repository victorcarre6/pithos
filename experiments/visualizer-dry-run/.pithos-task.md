# Durable facts — `src/audio_visualizer.py`

This file is read on every session regardless of which micro-rush is active; it never changes with the
rush. The actual task for this session is in the `## Current task` section injected alongside it -- do
not treat anything below as an instruction to implement.

`src/audio_visualizer.py` already implements, merged and validated:

- `split_bands(magnitudes)` -> `(bass, mid, treble)`, each a scalar float in `[0, 1]`.
- `smooth_levels(previous, current, alpha)` -> exponential blend of two `(bass, mid, treble)` triples,
  `previous_value * (1 - alpha) + current_value * alpha` per element.

Do not reimplement either from scratch, and do not change their existing signatures unless the current
task explicitly asks for it. Preserve both unchanged unless the current task's file list says otherwise.
No dependency beyond the standard library. Finish immediately once the current task's oracle would pass.
