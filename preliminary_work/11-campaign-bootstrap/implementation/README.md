# Implementation status

The current snapshot contains the reusable experiment template, its non-destructive creator and deterministic
tests. Long-running campaign activation remains gated by the selected model from `01-model-benchmark` and a
complete supervised dry-run.
